<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Message</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
</head>
<body>
    <%- include('nav') %>
    <div class="w-full flex justify-center">
        <div class="w-[50%] flex flex-col my-10">
            <h1>Create a Post</h1>
            <form action="/create-message" method="POST">
                <div class="flex justify-between p-2">
                    <label for="title">Post Title:</label>
                    <input class="w-full ml-5 border rounded-md shadow" type="text" name="title" placeholder="Your post's title...">
                </div>
                <div class="flex justify-between p-2">
                    <label for="content">Message</label>
                    <textarea class="w-full ml-5 border rounded-md shadow" placeholder="Post your messge here." name="content" id="content" rows="5" cols="33"></textarea>
                </div>
                <button>Post</button>
            </form>
        </div>  
    </div>  
    <% if (locals.errors) { %>
        <% for (var i =1; i <= locals.errors.length;  i++ ) { %>
            <p><%= locals.errors[i-1].msg %></p>
        <% }} %>
</body>
</html>